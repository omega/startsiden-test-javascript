
test("Finding class name works", function() {
    expect(1);
    equal(1 + 1, 2, "we can add numbers!");
});
load('t/root/static/test.js');

test("add function", function() {
    expect(1);
    equal(add(1, 1), 2, "we can add numbers with function");
});

